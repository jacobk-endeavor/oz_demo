# Pinned for oz-sandbox (python:3.12-slim). Plotly omitted for v1 (docs §12.2.1).
# Rebuild the image after bumping versions; CI publishes by git SHA + digest.
numpy==2.2.2
pandas==2.2.3
openpyxl==3.1.5
python-docx==1.1.2
reportlab==4.2.5
matplotlib==3.9.2
pillow==10.4.0
