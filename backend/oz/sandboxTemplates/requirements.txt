# Pinned for deterministic host + container renders (see docs/code-sandbox-and-artifact-generation.md §6.3, §12.2.1).
# Target runtime: python:3.12-slim (sandbox image). Pillow is a transitive dep of reportlab / image paths;
# pin it in the Docker image; keep this file to the three direct libraries the templates import.
openpyxl==3.1.5
python-docx==1.1.2
reportlab==4.4.2
