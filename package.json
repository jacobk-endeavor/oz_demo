{
  "name": "oz-demo",
  "private": true,
  "version": "0.0.0",
  "description": "Oz + Nebula demo workspace",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.721.0",
    "busboy": "^1.6.0"
  },
  "devDependencies": {
    "@types/busboy": "^1.5.4"
  },
  "scripts": {
    "setup": "npm install --include=dev && npm --prefix frontend install --include=dev",
    "dev": "npm run setup && npm --prefix frontend run dev -- --host 0.0.0.0",
    "build": "npm run setup && npm --prefix frontend run build",
    "start": "npm --prefix frontend run start",
    "preview": "npm --prefix frontend run preview -- --host 0.0.0.0",
    "guardrails:arch": "node scripts/ci/architecture-guardrails.mjs",
    "lint": "npm run setup && npm --prefix frontend run lint",
    "typecheck": "npm run setup && npm --prefix frontend run typecheck",
    "test": "npm run setup && npm --prefix frontend run test",
    "validate": "npm run guardrails:arch && npm run typecheck && npm run lint && npm run test && npm run build",
    "wiki:schema-review": "node scripts/wiki-schema-review.mjs --repoRoot ."
  }
}
