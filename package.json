{
  "name": "oz-demo",
  "private": true,
  "version": "0.0.0",
  "description": "Oz + Nebula demo workspace",
  "scripts": {
    "setup": "npm --prefix frontend install",
    "dev": "npm run setup && npm --prefix frontend run dev -- --host 0.0.0.0",
    "build": "npm run setup && npm --prefix frontend run build",
    "start": "npm --prefix frontend run start",
    "preview": "npm --prefix frontend run preview -- --host 0.0.0.0",
    "lint": "npm run setup && npm --prefix frontend run lint",
    "typecheck": "npm run setup && npm --prefix frontend run typecheck",
    "test": "npm run setup && npm --prefix frontend run test",
    "validate": "npm run typecheck && npm run lint && npm run test && npm run build"
  }
}
