# Schema review (automated)

- **Generated:** 2026-05-02T02:43:04.959Z
- **Repo root:** `/Users/jkali/.cursor/worktrees/ozdemo-qxk-824f7b43/Oz-Demo-12e6854ebde4`
- **Cadence:** every **50** ingested sources **or** **90** days (whichever first); see `docs/wiki-kb/track-b-wiki-layer.md` §9 Schema Agent, Q&A §12.
- **Emergent threshold:** ≥ **5** pages with the same frontmatter key not listed in `wiki/WIKI.md`

## Summary

- Markdown files under `wiki/` (excluding `_drafts/`): **24**
- Pages with YAML frontmatter: **7**
- Distinct frontmatter keys observed: **14**
- Emergent convention candidates (undocumented key, count ≥ 5): **0**

## Key frequency (all pages with frontmatter)

| Key | Count | Documented in WIKI.md |
| --- | ---: | --- |
| `confidence` | 7 | yes |
| `created` | 7 | yes |
| `related` | 7 | yes |
| `slug` | 7 | yes |
| `source_count` | 7 | yes |
| `tags` | 7 | yes |
| `title` | 7 | yes |
| `type` | 7 | yes |
| `updated` | 7 | yes |
| `doc_kind` | 6 | yes |
| `source_id` | 6 | yes |
| `brand` | 5 | yes |
| `year` | 3 | yes |
| `product_line` | 1 | yes |

## Emergent candidates

*(None at ≥5 pages. Lint-style “novel keys” report would list keys here when usage crosses the threshold.)*

## Undocumented keys in use (any count)

*All observed keys appear in the contract.*

## WIKI.md vs current wiki (manual checklist)

- **Contract file:** `wiki/WIKI.md` — canonical required fields match current source + synthesis pages (see key table above).
- **Layout:** `wiki/WIKI.md` lists top-level dirs; repo includes scaffold `README.md` placeholders under entities/concepts without frontmatter — expected until Index/Linker populate.
- **Slugs:** source pages use path-shaped slugs (e.g. `sources/<slug>`) as generated by ingest — consistent with “unique slug” wording.

## Proposed WIKI.md updates (pending human approval)

1. **No emergent keys above threshold** — optional: tighten wording only if product owners want stricter typing for manifest-derived fields.
2. **When catalog / SKU / recommendation pages appear:** extend the contract with optional keys from `docs/wiki-kb/track-b-wiki-layer.md` (e.g. `product_line_code`, `catalog_refresh`, `created_reason` for SKU pages, `methodology_version` on recommendations methodology) — add only after keys appear in real pages.
3. **Re-run this report** after bulk ingest milestones (50 sources) or on a 90-day timer; merge emergent candidates into `WIKI.md` when Lint would flag them.

---
Contract keys checked (sync with `wiki/WIKI.md`): brand, concept_kind, confidence, created, doc_kind, entity_kind, product_line, related, slug, source_count, source_id, tags, title, type, updated, year.
